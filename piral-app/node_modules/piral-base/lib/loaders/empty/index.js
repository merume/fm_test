"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const utils_1 = require("../../utils");
/**
 * Loads an empty pilet as a placeholder (something went wrong beforehand).
 * @param entry The pilet's entry.
 * @param _config The loader configuration.
 * @returns The evaluated pilet that can now be integrated.
 */
function loader(entry, _config) {
    const { name, spec = 'vx', dependencies = {} } = entry, rest = tslib_1.__rest(entry, ["name", "spec", "dependencies"]);
    const meta = Object.assign({ name, version: '', spec,
        dependencies, config: {}, link: '', basePath: '' }, rest);
    console.warn('Empty pilet found!', name);
    return (0, utils_1.promisify)(Object.assign(Object.assign({}, meta), utils_1.emptyApp));
}
exports.default = loader;
//# sourceMappingURL=index.js.map