"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.inspectPilet = void 0;
const lifecycle_1 = require("./lifecycle");
function inspectPilet(meta) {
    const inBrowser = typeof document !== 'undefined';
    if (inBrowser && 'link' in meta && meta.spec === 'v2') {
        return ['v2', meta, lifecycle_1.setupSinglePilet];
    }
    else if (inBrowser && 'requireRef' in meta && meta.spec !== 'v2') {
        return ['v1', meta, lifecycle_1.setupSinglePilet];
    }
    else if (inBrowser && 'bundle' in meta && meta.bundle) {
        return ['bundle', meta, lifecycle_1.setupPiletBundle];
    }
    else if ('hash' in meta) {
        return ['v0', meta, lifecycle_1.setupSinglePilet];
    }
    else {
        return ['unknown', meta, lifecycle_1.setupSinglePilet];
    }
}
exports.inspectPilet = inspectPilet;
//# sourceMappingURL=inspect.js.map