"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.detectClients = exports.isWrapperClient = exports.clients = void 0;
const path_1 = require("path");
const io_1 = require("../io");
const lerna = require("./lerna");
const npm = require("./npm");
const pnp = require("./pnp");
const pnpm = require("./pnpm");
const rush = require("./rush");
const yarn = require("./yarn");
exports.clients = {
    lerna,
    npm,
    pnp,
    pnpm,
    rush,
    yarn,
};
const directClients = ['npm', 'pnp', 'yarn', 'pnpm'];
function isWrapperClient(client) {
    return !directClients.includes(client);
}
exports.isWrapperClient = isWrapperClient;
function detectClients(root) {
    return __awaiter(this, void 0, void 0, function* () {
        const packageJson = yield (0, io_1.findFile)((0, path_1.resolve)(root, '..'), 'package.json');
        const stopDir = packageJson ? (0, path_1.dirname)(packageJson) : undefined;
        return yield Promise.all(Object.keys(exports.clients).map((client) => __awaiter(this, void 0, void 0, function* () {
            const result = yield exports.clients[client].detectClient(root, stopDir);
            return {
                client,
                result,
            };
        })));
    });
}
exports.detectClients = detectClients;
//# sourceMappingURL=index.js.map