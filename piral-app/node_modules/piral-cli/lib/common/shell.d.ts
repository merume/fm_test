import { NpmClientType } from '../types';
export declare function installPiralInstance(usedSource: string, baseDir: string, rootDir: string, npmClient: NpmClientType): Promise<[name: string, version: string]>;
