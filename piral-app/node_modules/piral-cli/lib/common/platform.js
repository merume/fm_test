"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.configurePlatform = void 0;
const log_1 = require("./log");
function configurePlatform(target = {}) {
    const { platform = 'web' } = target, options = __rest(target, ["platform"]);
    if (platform === 'web') {
        const { setup } = require('../platforms/web');
        return setup(options);
    }
    else if (platform === 'node') {
        const { setup } = require('../platforms/node');
        return setup(options);
    }
    return (0, log_1.fail)('platformNotSupported_0190', platform);
}
exports.configurePlatform = configurePlatform;
//# sourceMappingURL=platform.js.map